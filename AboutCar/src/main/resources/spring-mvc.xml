<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. 加载外部配置文件（数据库配置、自定义配置），忽略无法解析的属性（避免配置冲突） -->
    <context:property-placeholder
            location="classpath:db.properties,classpath:config.properties"
            ignore-unresolvable="true"/>

    <!-- 2. 精准扫描Controller相关注解（提高性能，避免重复扫描） -->
    <context:component-scan base-package="com.example.controller" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:include-filter type="annotation" expression="org.springframework.web.bind.annotation.RestController"/>
        <context:include-filter type="annotation" expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
    </context:component-scan>

    <!-- 3. 扫描拦截器、全局异常处理器等非Controller组件（确保@Component注解生效） -->
    <context:component-scan base-package="com.example.common"/>

    <!-- 4. 开启SpringMVC注解驱动，配置JSON消息转换器（返回JSON格式，解决中文乱码） -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 5. 放行静态资源（JS、CSS、图片等，避免SpringMVC拦截） -->
    <mvc:default-servlet-handler/>

    <!-- 6. 注册JWT拦截器（将拦截器声明为Spring Bean） -->
    <bean id="jwtInterceptor" class="com.example.common.JWTInterceptor"/>

    <!-- 7. 配置拦截器链（核心：拦截规则 + 排除规则，确保无权限接口正常访问） -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 拦截所有请求 -->
            <mvc:mapping path="/**"/>

            <mvc:exclude-mapping path="/login/**"/>
            <mvc:exclude-mapping path="/register/**"/>
            <mvc:exclude-mapping path="/findPwd/**"/>
            <mvc:exclude-mapping path="/error"/>
            <mvc:exclude-mapping path="/static/**"/>
            <mvc:exclude-mapping path="/email/**"/>
            <mvc:exclude-mapping path="/question/**"/>
            <mvc:exclude-mapping path="/examRecord/**"/>

            <!-- 引用JWT拦截器Bean -->
            <ref bean="jwtInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>



</beans>